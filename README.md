# SENTINODE
An interactive, visual IDE for smart contract auditing and logic mapping. SENTINODE unifies static analysis (Slither) and structural mapping (Traverse) into a navigable 'logic-flow' canvas, enabling auditors to visually trace state transitions, call-graphs, and execution risks across any EVM-compatible protocol.
